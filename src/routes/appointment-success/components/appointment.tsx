import PanelSuccessAppointment from '@/widgets/panel-success-appointment';
import { memo } from 'react';

const AppointmentSuccessPage = memo(() => <PanelSuccessAppointment />);

export default AppointmentSuccessPage;
